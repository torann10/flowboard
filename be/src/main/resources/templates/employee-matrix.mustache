<!DOCTYPE html>
<html lang="hu">
<head>
    <meta charset="utf-8"/>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8"/>
    <meta name="viewport" content="width=device-width,initial-scale=1"/>
    <title>Munkavállalói mátrix</title>

    <style>
        @page {
            size: A4 landscape;
            margin: 20mm;
        }

        /* Fallbacks: first try embedded font, then system fonts */
        body {
          font-family: "PT Mono";
          font-size: 11pt;
          color: #1f2933;
          line-height: 1.45;
          margin: 0;
        }

        h1 {
            text-align: center;
            color: #2b4d8a;
            margin-bottom: 20px;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            border: 2px solid #2b4d8a;
        }

        th, td {
            padding: 8px;
            text-align: center;
            border: 1px solid #a4c0ff;
        }

        /* First row = Matrix header */
        tr:first-child td {
            background-color: #2b4d8a;
            color: white;
            font-weight: bold;
            border-color: #2b4d8a;
        }

        /* Zebra stripes */
        tr:nth-child(even) td {
            background-color: #eef3ff;
        }
    </style>
</head>

<body>

<h1>Munkavállalói mátrix</h1>

<table>
    {{#matrix}}
    <tr>
        {{#cells}}
        <td>{{.}}</td>
        {{/cells}}
    </tr>
    {{/matrix}}
</table>

</body>
</html>
